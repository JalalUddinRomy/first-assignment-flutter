void main(){
  var alphabet=['a','b','c','d'];
  //list properties
  var result=alphabet.length;
  var firstlistitem=alphabet.first;
  var secListItem=alphabet.reversed;
  var empty=alphabet.isEmpty;
  var notempty=alphabet.isNotEmpty;
  print(result);
  print(firstlistitem);
  print(secListItem);
  print(empty);
  print(notempty);
}
